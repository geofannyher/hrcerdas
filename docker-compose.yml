version: '3.9'
services:
  frontend_headhunter:
    container_name: 'frontend_headhunter'
    build: .
    command: yarn start
    ports:
      - 3001:3000
    volumes:
      - ./:/app/
      - /app/node_modules